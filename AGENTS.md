# 말씀 자료실 (malsseum)

주일·수요 말씀을 모아 보는 페이지. <https://patrickdfg.github.io/malsseum/> 로 올라간다.
원래 `sayeon` 저장소의 `malsseum/` 아래 있었는데, 음성이 커져서(약 570MB)
2026-09-14 에 이 저장소로 떼어 냈다. 두 저장소는 같은 호스트(patrickdfg.github.io)라
브라우저가 보기엔 같은 출처다 — 그래서 설정·암호·검색이 그대로 이어진다.

## 대화 규칙
- **시작할 때 먼저 `git pull`**, 끝낼 때 **커밋하고 푸시**. (집·사무실 두 대에서 쓴다)

## 구조
- `index.html` — 말씀 뷰어 (탭에서 성령사연 `/sayeon/`, 월명동 `/sayeon/stones/` 로 간다)
- `malsseum.json.enc` — 잠긴 원고. `audio/*.mp3` + `audio/sync.json` — 음성과 문단 시간표
- `crypt.js`·`settings.js`·`analytics*.js` — sayeon 과 같은 파일(자기완결로 복사해 둠)
- `crypt.json`·`check.enc` — sayeon 과 **같은 소금**이라 같은 암호(7125)로 풀린다

## 원고와 사진은 잠겨 있다
- `.enc` 만 올린다. 브라우저는 `crypt.js`, 도구는 `tools/crypt.py` 로 푼다.
- 도구를 돌릴 때는 환경변수 `SAYEON_PASS`(=7125), 깃허브 일꾼은 저장소 비밀값
  `SAYEON_PASS` 에서 받는다. **이 저장소에도 그 비밀값을 넣어야** 자동 게시가 된다.

## 말씀 추가하기
1. HWP 원고 → `python tools/import_malsseum_hwp.py <파일> --no <번호> --write` (SAYEON_PASS 필요)
2. 현수 음성: `malsseum-tts-request.json` 에 `{"numbers":[...]}` 올리거나 Actions 에서 돌린다.
   한 편 끝날 때마다 저장되고, 이미 만든 편은 건너뛴다.
3. 큰 육성 녹음은 `.upload/` 로 조각내 올리면 `assemble-upload.yml` 이 합치고
   25MB 넘으면 48kbps 모노로 줄인다.
4. **음성을 넣었으면 문단 시간표를 만든다.** Actions 의 "말씀 문단 시간표 만들기"
   (`build-sync.yml`) 를 돌린다. 안 만들면 소리는 나는데 화면이 문단을 안 따라간다.

## 문단 시간표 (audio/sync.json)

`{"편번호": [문단마다 시작 시각(초)]}` 이다. 뷰어는 재생 위치를 보고 그 문단을 짚는다.
뷰어는 **음성 파일 이름(`0920`)을 먼저 찾고, 없으면 편 번호(`35`)** 를 본다.
옛 자료 때문에 두 가지 키가 섞여 있는데 둘 다 읽는다.

- 만드는 도구는 `tools/build_sync.py`. Whisper 로 받아쓴 뒤 원고에 맞춰 붙인다.
  이미 있는 편은 건너뛴다. 다시 만들려면 `sync.json` 에서 그 항목을 지우고 돌린다.
- **sayeon 의 `tools/build_sync.py` 는 말씀에 쓰지 마라.** 그쪽은 말씀이
  `sayeon/malsseum/` 아래 있던 옛 구조를 본다(2026-09-14 에 갈라졌다).
- **맞추지 못하면 대개 녹음이 그 편 원고가 아니다.** 파일을 잘못 넣었는지 먼저 보라.
- 받아쓴 중간 결과는 `.seg_cache/` 에 남는다. 저장소에는 안 올린다.

## 편 번호(no)
- 화면은 날짜(`short`, 월/일) 순으로 정렬한다. `no` 는 안 보이는 고유 번호일 뿐.
- 8~9월 육성 13편이 no 1~12·33, 1~7월 TTS 54편이 no 100~153.

## 새 말씀 알림

`malsseum.json.enc` 가 main 에 올라오면 `push-notify.yml` 이 `tools/send_push.py` 를
돌려 "새 말씀 — 9월 20일 주일말씀 이(가) 올라왔습니다" 알림을 보낸다.
누르면 `/malsseum/#n=편번호` 로 그 편이 열린다.

**명단은 성령사연과 하나다.** 두 사이트가 같은 호스트라 브라우저가 보기엔 같은 출처이고,
알림 명단(Supabase `push_subscriptions`)도 표 하나를 같이 쓴다. 그래서
**따로 켜고 끌 수 없다** — '받기'를 누른 기기는 사연도 말씀도 함께 받는다.
나누려면 명단 표에 종류 칸을 더해야 하고, 이미 받기를 누른 기기도 다시 눌러야 한다.

- 서비스 워커는 `sayeon` 저장소의 `/sayeon/sw.js` 하나뿐이다. 이 저장소에는 `sw.js` 가 없다.
  구독이 그 워커에 매여 있어서, 말씀 알림도 그 워커가 띄운다.
  그래서 **말씀 알림 모양이나 누를 때 동작을 고치려면 `sayeon/sw.js` 를 고쳐야 한다.**
- `settings.js` 는 sayeon 것과 **같은 파일**이다(받기 단추 포함). 한쪽을 고치면 같이 복사한다.
- **같은 편은 한 번만 보낸다.** 보내기 전에 Supabase `push_sent` 에 `mal-<편번호>` 를 적는다.
  `mal-` 을 붙이는 것은 사연 편 번호와 섞이지 않게 하기 위함이다.
- **사이트에 새 원고가 올라간 것을 확인하고 보낸다**(최대 10분 기다림).
- 저장소 비밀값 `VAPID_PRIVATE_KEY`(알림 서명), `PUSH_TOKEN`(명단 읽기) 이 있어야 한다.
  **없으면 알림만 건너뛰고 게시는 그대로 된다.** 두 값은 sayeon 저장소와 같은 값이다.
- 알림 내용에는 **편 제목만** 넣는다. 원고를 잠가 둔 뜻을 살려 본문은 넣지 않는다.
- 손으로 시험할 때는 Actions 의 "새 말씀 알림" 을 돌린다(시험 알림이 기본값).

## HTML 을 파이썬으로 고치면 끝나고 문법 검사(node --check)를 하자.
